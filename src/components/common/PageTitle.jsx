import React from "react";

export const PageTitle = ({ title }) => {
  return (
    <div className="justify-center my-4 text-xl text-blue-800 capitalize font-medium opacity-80">
      {title}
    </div>
  );
};
